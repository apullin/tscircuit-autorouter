diff --git a/home/pullin/.perf-scratch/hypot-backup/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/DuplicateCongestedPortSolver.ts b/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/DuplicateCongestedPortSolver.ts
index e9f0f089..f9b5f534 100644
--- a/home/pullin/.perf-scratch/hypot-backup/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/DuplicateCongestedPortSolver.ts
+++ b/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/DuplicateCongestedPortSolver.ts
@@ -1,3 +1,4 @@
+const __h2 = (a: number, b: number) => Math.sqrt(a * a + b * b)
 import type { SerializedHyperGraph } from "@tscircuit/hypergraph"
 import { BaseSolver } from "@tscircuit/solver-utils"
 import { loadSerializedHyperGraph } from "./compat/loadSerializedHyperGraph"
@@ -75,10 +76,10 @@ const getBoundaryKey = (
   port: Pick<SerializedPort, "region1Id" | "region2Id">,
 ) => [port.region1Id, port.region2Id].sort().join("\u0000")
 
-const getDistance = (a: Point, b: Point) => Math.hypot(a.x - b.x, a.y - b.y)
+const getDistance = (a: Point, b: Point) => __h2(a.x - b.x, a.y - b.y)
 
 const normalize = (point: Point): Point | undefined => {
-  const length = Math.hypot(point.x, point.y)
+  const length = __h2(point.x, point.y)
   if (length <= EPSILON) return undefined
   return {
     x: point.x / length,
diff --git a/home/pullin/.perf-scratch/hypot-backup/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/bus-solver/BusBoundaryPlanner.ts b/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/bus-solver/BusBoundaryPlanner.ts
index 0f878f22..cf7a790b 100644
--- a/home/pullin/.perf-scratch/hypot-backup/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/bus-solver/BusBoundaryPlanner.ts
+++ b/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/bus-solver/BusBoundaryPlanner.ts
@@ -1,3 +1,4 @@
+const __h2 = (a: number, b: number) => Math.sqrt(a * a + b * b)
 import type { TinyHyperGraphProblem, TinyHyperGraphTopology } from "../core"
 import type { PortId, RegionId } from "../types"
 import type { BusTraceOrder } from "./deriveBusTraceOrder"
@@ -409,7 +410,7 @@ export class BusBoundaryPlanner {
     let tangentY =
       this.options.topology.portY[toPortId] -
       this.options.topology.portY[fromPortId]
-    let tangentLength = Math.hypot(tangentX, tangentY)
+    let tangentLength = __h2(tangentX, tangentY)
 
     if (tangentLength <= BUS_CANDIDATE_EPSILON) {
       tangentX =
@@ -418,7 +419,7 @@ export class BusBoundaryPlanner {
       tangentY =
         this.options.topology.regionCenterY[toRegionId] -
         this.options.topology.regionCenterY[fromRegionId]
-      tangentLength = Math.hypot(tangentX, tangentY)
+      tangentLength = __h2(tangentX, tangentY)
     }
 
     if (tangentLength <= BUS_CANDIDATE_EPSILON) {
diff --git a/home/pullin/.perf-scratch/hypot-backup/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/bus-solver/deriveBusTraceOrder.ts b/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/bus-solver/deriveBusTraceOrder.ts
index 8c4c3269..1d1437ff 100644
--- a/home/pullin/.perf-scratch/hypot-backup/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/bus-solver/deriveBusTraceOrder.ts
+++ b/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/bus-solver/deriveBusTraceOrder.ts
@@ -1,3 +1,4 @@
+const __h2 = (a: number, b: number) => Math.sqrt(a * a + b * b)
 import type { TinyHyperGraphProblem, TinyHyperGraphTopology } from "../core"
 import type { RouteId } from "../types"
 
@@ -59,7 +60,7 @@ export const deriveBusTraceOrder = (
 
   let directionX = endCenterX - startCenterX
   let directionY = endCenterY - startCenterY
-  const directionLength = Math.hypot(directionX, directionY)
+  const directionLength = __h2(directionX, directionY)
 
   if (directionLength <= EPSILON) {
     directionX = 1
diff --git a/home/pullin/.perf-scratch/hypot-backup/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/bus-solver/geometry.ts b/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/bus-solver/geometry.ts
index 651cb8de..ecf3d8e6 100644
--- a/home/pullin/.perf-scratch/hypot-backup/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/bus-solver/geometry.ts
+++ b/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/bus-solver/geometry.ts
@@ -1,3 +1,4 @@
+const __h2 = (a: number, b: number) => Math.sqrt(a * a + b * b)
 import type { TinyHyperGraphTopology } from "../core"
 import type { PortId } from "../types"
 
@@ -31,7 +32,7 @@ const getPointToPointDistance = (
   y1: number,
   x2: number,
   y2: number,
-) => Math.hypot(x1 - x2, y1 - y2)
+) => __h2(x1 - x2, y1 - y2)
 
 const getPointToSegmentDistance = (
   px: number,
@@ -115,7 +116,7 @@ export const getPortProgressAlongPolyline = (
     const end = getPortPoint(topology, polylinePortIds[index]!)
     const abX = end.x - start.x
     const abY = end.y - start.y
-    const abLength = Math.hypot(abX, abY)
+    const abLength = __h2(abX, abY)
 
     if (abLength <= EPSILON) {
       continue
diff --git a/home/pullin/.perf-scratch/hypot-backup/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/core.ts b/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/core.ts
index 5cb0b763..0d4b289c 100644
--- a/home/pullin/.perf-scratch/hypot-backup/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/core.ts
+++ b/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/core.ts
@@ -1,3 +1,4 @@
+const __h2 = (a: number, b: number) => Math.sqrt(a * a + b * b)
 import { BaseSolver } from "@tscircuit/solver-utils"
 import type { GraphicsObject } from "graphics-debug"
 import { convertToSerializedHyperGraph } from "./compat/convertToSerializedHyperGraph"
@@ -457,7 +458,7 @@ export class TinyHyperGraphSolver extends BaseSolver {
           const dx = portX[portId] - endX
           const dy = portY[portId] - endY
           portHCostToEndOfRoute[portId * problem.routeCount + routeId] =
-            Math.hypot(dx, dy) * this.DISTANCE_TO_COST
+            __h2(dx, dy) * this.DISTANCE_TO_COST
         }
       }
     }
@@ -1493,7 +1494,7 @@ export class TinyHyperGraphSolver extends BaseSolver {
     const endPortId = this.problem.routeEndPort[this.state.currentRouteId!]
     const dx = portX[neighborPortId] - portX[endPortId]
     const dy = portY[neighborPortId] - portY[endPortId]
-    return Math.hypot(dx, dy) * this.DISTANCE_TO_COST
+    return __h2(dx, dy) * this.DISTANCE_TO_COST
   }
 
   override visualize(): GraphicsObject {
diff --git a/home/pullin/.perf-scratch/hypot-backup/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/distance-aware-tiny-hypergraph-solver.ts b/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/distance-aware-tiny-hypergraph-solver.ts
index 5b61549a..b2c0f7e6 100644
--- a/home/pullin/.perf-scratch/hypot-backup/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/distance-aware-tiny-hypergraph-solver.ts
+++ b/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/distance-aware-tiny-hypergraph-solver.ts
@@ -1,3 +1,4 @@
+const __h2 = (a: number, b: number) => Math.sqrt(a * a + b * b)
 import {
   type Candidate,
   TinyHyperGraphSolver,
@@ -42,7 +43,7 @@ export class DistanceAwareTinyHyperGraphSolver extends TinyHyperGraphSolver {
     const currentPortId = currentCandidate.portId
     const dx = portX[currentPortId]! - portX[neighborPortId]!
     const dy = portY[currentPortId]! - portY[neighborPortId]!
-    return baseCost + Math.hypot(dx, dy) * this.DISTANCE_TO_COST
+    return baseCost + __h2(dx, dy) * this.DISTANCE_TO_COST
   }
 
   override onPathFound(finalCandidate: Candidate): void {
diff --git a/home/pullin/.perf-scratch/hypot-backup/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/poly.ts b/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/poly.ts
index 5cfbda01..f68ab782 100644
--- a/home/pullin/.perf-scratch/hypot-backup/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/poly.ts
+++ b/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/poly.ts
@@ -1,3 +1,4 @@
+const __h2 = (a: number, b: number) => Math.sqrt(a * a + b * b)
 import type { SerializedHyperGraph } from "@tscircuit/hypergraph"
 import type { PipelineStep } from "@tscircuit/solver-utils"
 import type { GraphicsObject } from "graphics-debug"
@@ -237,7 +238,7 @@ const computePolygonGeometry = (polygon: ConvexPolygon) => {
     signedDoubleArea += cross
     centroidXNumerator += (point.x + nextPoint.x) * cross
     centroidYNumerator += (point.y + nextPoint.y) * cross
-    perimeter += Math.hypot(nextPoint.x - point.x, nextPoint.y - point.y)
+    perimeter += __h2(nextPoint.x - point.x, nextPoint.y - point.y)
     minX = Math.min(minX, point.x)
     maxX = Math.max(maxX, point.x)
     minY = Math.min(minY, point.y)
diff --git a/home/pullin/.perf-scratch/hypot-backup/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/region-graph/graph.ts b/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/region-graph/graph.ts
index bc4d2a10..f9718f5a 100644
--- a/home/pullin/.perf-scratch/hypot-backup/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/region-graph/graph.ts
+++ b/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/lib/region-graph/graph.ts
@@ -1,3 +1,4 @@
+const __h2 = (a: number, b: number) => Math.sqrt(a * a + b * b)
 import type { TinyHyperGraphProblem, TinyHyperGraphTopology } from "../core"
 import type { NetId, PortId, RegionId } from "../types"
 
@@ -140,7 +141,7 @@ export const createRegionGraph = (
       regionIdB,
       portIds: [portId],
       representativePortId: portId,
-      centerDistance: Math.hypot(dx, dy),
+      centerDistance: __h2(dx, dy),
     })
   }
 
diff --git a/home/pullin/.perf-scratch/hypot-backup/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/tests/solver/duplicate-congested-port-solver.test.ts b/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/tests/solver/duplicate-congested-port-solver.test.ts
index ec2c1e55..df32af97 100644
--- a/home/pullin/.perf-scratch/hypot-backup/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/tests/solver/duplicate-congested-port-solver.test.ts
+++ b/home/pullin/personal/awt-ts2/node_modules/tiny-hypergraph/tests/solver/duplicate-congested-port-solver.test.ts
@@ -1,3 +1,4 @@
+const __h2 = (a: number, b: number) => Math.sqrt(a * a + b * b)
 import { expect, test } from "bun:test"
 import type { SerializedHyperGraph } from "@tscircuit/hypergraph"
 import { loadSerializedHyperGraph } from "lib/compat/loadSerializedHyperGraph"
@@ -180,7 +181,7 @@ test("duplicate congested port solver duplicates independently reused ports in l
     x: getNumber(duplicatePort.d?.x) - sourcePoint.x,
     y: getNumber(duplicatePort.d?.y) - sourcePoint.y,
   }
-  const duplicateDistance = Math.hypot(duplicateVector.x, duplicateVector.y)
+  const duplicateDistance = __h2(duplicateVector.x, duplicateVector.y)
   const crossProduct =
     nearestVector.x * duplicateVector.y - nearestVector.y * duplicateVector.x
 
